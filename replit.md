# Photo Gallery

This is a dependency-free static website built with plain HTML, CSS, and JavaScript. It does not use a framework, build step, or package manager.

## Run

The **Start application** workflow serves the project on port 5000 with:

```sh
python3 -m http.server 5000 --bind 0.0.0.0
```

Open the Replit web preview to view the gallery.